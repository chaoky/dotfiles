- clean: ['~']

- link:
    ~/.ssh: ssh
    ~/.vscode-oss: vscode-oss
    ~/.themes: gtkThemes
    ~/.zsh_plugins.txt: zsh_plugins
- shell:
    - [echo 'source ~/.zsh_plugins.sh' > ~/.zshrc && antibody bundle < ~/.zsh_plugins.txt > ~/.zsh_plugins.sh, loading plugins]
    - [git config --global user.email "levimanga@gmail.com" && git config --global user.name "lordie", git config]
      
